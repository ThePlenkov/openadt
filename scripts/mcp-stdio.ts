#!/usr/bin/env bun
/**
 * Repo-root stdio MCP entry for IDE / agent MCP config.
 * Invoked as: bun scripts/mcp-stdio.ts  (or nx run adt-mcp:serve-stdio)
 */
import '../packages/adt-mcp/src/mcp-stdio-entry.ts'
