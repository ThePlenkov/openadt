{
  "name": "adt-lsp-mcp",
  "root": "packages/adt-lsp-mcp",
  "sourceRoot": "packages/adt-lsp-mcp/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun run build",
        "cwd": "packages/adt-lsp-mcp"
      },
      "outputs": ["{projectRoot}/dist"]
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun test packages/adt-lsp-mcp/src"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bunx tsc -p packages/adt-lsp-mcp/tsconfig.json --noEmit"
      }
    },
    "serve-stdio": {
      "executor": "nx:run-commands",
      "cache": false,
      "options": {
        "command": "bun scripts/mcp-adt-lsp.ts",
        "cwd": "{workspaceRoot}",
        "forwardAllArgs": true
      }
    }
  }
}
